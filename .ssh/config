# Include a literal list because globs and/or .d-style doesn't feel right here.
# If tokens were supported here, I'd use config.%L instead of config.local.
Include config.local
Include config.priv
Include config.work

Host *
	ForwardX11 no
	ForwardAgent yes
	StrictHostKeyChecking no
	NoHostAuthenticationForLocalhost yes
